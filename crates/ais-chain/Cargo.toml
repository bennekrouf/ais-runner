[package]
name = "ais-chain"
version = "0.1.0"
edition = "2021"
description = "Discover workflow chains in Azure Logic Apps Standard projects"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
